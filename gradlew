#!/bin/sh
# Gradle wrapper script (Unix)
# Generated for SWWorth

APP_NAME="Gradle"
APP_BASE_NAME=`basename "$0"`
APP_HOME="`pwd -P`"

GRADLE_OPTS="$GRADLE_OPTS"
export GRADLE_OPTS

exec "$APP_HOME/gradlew" "$@"
